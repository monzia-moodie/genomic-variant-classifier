# SESSION 2026-05-09: C3.6 hotfix + C4-prep

## Goal

Prepare for the C4 pickle migration (the last unfinished step in the
consolidate-package-layout migration series) by:

1. Auditing prerequisites: free disk, populated `scripts/migrate_pickles.py`,
   5 production joblibs, lineage SHA256 invariant, namespace import smoke.
2. Fixing any blockers in `scripts/migrate_pickles.py`.
3. Building the §F-8 equivalence-check fixture
   (`tests/fixtures/migration_smoke.parquet`).
4. Committing and pushing in two clean commits.

## Outcome

**SUCCESS.** Two commits on `origin/main`:

- `e0f4c6e` (chore: migration C3.6) — finish bare-import rewrite +
  add `agent_layer/__init__.py`. 9 files changed, 44 insertions, 44 deletions.
- `e34ce7b` (chore: migration C4-prep) — bind `agent_layer` in
  `install_compat_aliases` + add `migration_smoke` fixture. 2 files changed,
  1 insertion (the L119 line), one new force-added parquet fixture.

C4 (the real pickle migration) preconditions are now met. Stage 5 ready
to schedule.

## Stage-by-stage timeline

### Stage 1: Pre-condition audit (`fc7f63a` HEAD, clean tree)

- **A. Free disk**: 179.6 GB (need >= 25 for `.bak` originals)
- **B. `scripts/migrate_pickles.py`**: 22716 bytes (populated, not placeholder)
- **C. Frozen fixture**: MISSING (to be built in Stage 3)
- **D. Production joblibs**: 5/5 present at expected sizes
- **E. SHA256 lineage match**: True
  (`models/v1/ensemble_v1.joblib` and
  `experiments/2026-04-04_03-39/ensemble_v1.joblib` both
  `64FEF61170E98FF722C338734819008A4F8307111D6AB97EC6BE14A29ADCEE23`,
  matches spec invariant)
- **F. Namespace imports**: `genomic_variant_classifier.models.variant_ensemble`
  OK; `genomic_variant_classifier.api.pipeline` OK

**Verdict**: Stages 2–4 unblocked.

### Stage 2: Spec compliance audit

- **A. Public API symbols**: all 6 found at expected lines
  (`install_compat_aliases` L105, `verify_no_legacy_modules` L167,
  `migrate_one` L224, `equivalence_check` L316, `delete_one` L345,
  `main` L486)
- **B. Module-level constants** (`TARGETS`, `DELETIONS`): regex MISSING (false
  red — they're computed `Path()`-based, importable, and content matches spec)
- **C. Spec mechanism keywords**: all 8 found (`pkgutil.walk_packages`,
  `sys.modules[`, `joblib.load`, `joblib.dump`, `predict_proba`,
  `assert_allclose`, `subprocess`, `sha256`)
- **D. Functional smoke**: **FAILED** at L122
  ```
  AttributeError: module 'genomic_variant_classifier' has no attribute 'agent_layer'
  ```
- **E. TARGETS / DELETIONS values**: 5/5, 1/1; paths match spec

**Diagnosis (Stage 2.5)**: bare `import genomic_variant_classifier as _new_root`
does not bind subpackage attributes. Need explicit `import
genomic_variant_classifier.agent_layer` before the `_new_root.agent_layer`
access at L122.

### Stage 2.5b: Patch L119

Inserted via `agent_data/c4_fix_install_compat.py` (3576 B):

```python
    import genomic_variant_classifier as _new_root
    import genomic_variant_classifier.agent_layer  # bind subpackage attr on _new_root before .agent_layer access below

    # Top-level aliases (necessary, not sufficient on their own).
    sys.modules.setdefault("src", _new_root)
    sys.modules.setdefault("agent_layer", _new_root.agent_layer)
```

File grew from 22716 B -> 22837 B (+121 B), CRLF preserved.

Smoke retest:
- `install_compat_aliases`: OK
- `verify_no_legacy_modules(np.array)`: `[]`
- `src.*` aliases: 53 (low; expected > 50, so PASS)
- `agent_layer.*` aliases: **1** (expected > 4; FAIL)

Only 1 `agent_layer.*` alias = the top-level `setdefault` itself. Walk loop
registered ZERO `agent_layer.*` children. Something in the walk is skipping
that subtree.

### Stage 2.5c: Diagnose walk anomaly (`agent_data/c4_diagnose_walk.py`)

Filesystem inventory:
```
PKG  NO  __init__.py  agent_layer/.            <- the smoking gun
PKG  YES (170 B)      agent_layer/agents/
```

`importlib.util.find_spec('genomic_variant_classifier.agent_layer')`:
```
spec.name:                  genomic_variant_classifier.agent_layer
spec.origin:                None                                 <- NAMESPACE
spec.submodule_search_locations: _NamespacePath([...])
spec.has_location:          False
```

Walks:
- C1 (over `genomic_variant_classifier.__path__`): 52 entries, **0 under `agent_layer.*`**
- C2 (over `agent_layer.__path__` directly): 27 entries, all expected children

**Verdict**: PEP 420 namespace packages are NOT auto-recursed by
`pkgutil.walk_packages` without `__init__.py`. C2 spec called for empty
`__init__.py` at the new location; it never landed (C1 sweep miss).

### Stage 2.5d (first run, FAILED): Add `__init__.py`, retest

Created `src/genomic_variant_classifier/agent_layer/__init__.py` (0 B).
`find_spec` now returns REGULAR.

Smoke retest exposes a SECOND C-series regression:
```
[migrate_pickles] WARN: import failures during alias installation:
  agents.base_agent              ModuleNotFoundError: No module named 'config'
  agents.data_freshness_agent    ModuleNotFoundError: No module named 'agents'
  agents.interpretability_agent  ModuleNotFoundError: No module named 'agents'
  agents.literature_scout_agent  ModuleNotFoundError: No module named 'agents'
  agents.training_lifecycle_agent ModuleNotFoundError: No module named 'agents'
  agent_layer.orchestrator       ModuleNotFoundError: No module named 'config'
[migrate_pickles] continuing; joblib.load will surface real problems.
  src.* aliases: 75
  agent_layer.* aliases: 22 (< threshold 25)
```

**Diagnosis**: 6 modules have bare top-level imports of `agents`/`config`
that should have been rewritten by C3 patterns 6 and 7. Pre-migration
they worked because `find_packages()` discovered `agent_layer/`'s
subpackages as top-level. Post-C1 they break.

Batch ABORTED at Stage 2.5d. C3.6 hotfix needed.

### Stage 2.5e: C3.6 hotfix

Sweep `agent_data/c4_fix_bare_imports.py` applies C3 spec patterns 6 and 7
(with added `\b` word-boundary hardening) over
`src/genomic_variant_classifier/**/*.py`:

```
=== APPLY ===
Files scanned: 86

agent_layer/agents/base_agent.py                 (3 lines, +117 B)
agent_layer/agents/data_freshness_agent.py       (4 lines, +156 B)
agent_layer/agents/interpretability_agent.py     (4 lines, +156 B)
agent_layer/agents/literature_scout_agent.py     (4 lines, +156 B)
agent_layer/agents/training_lifecycle_agent.py   (4 lines, +156 B)
agent_layer/orchestrator.py                      (7 lines, +273 B)
agent_layer/run_agents.py                        (1 line,   +39 B)
agent_layer/test_message_bus.py                 (17 lines, +663 B)

Total: 8 files, 44 lines, +1716 bytes.
```

Note: 8 files matched, not just the 6 from walk_failures.
`run_agents.py` and `test_message_bus.py` had bare imports that didn't
surface as walk_failures (their module-level imports succeeded somehow,
likely sys.modules cache state from prior imports), but the static regex
caught them anyway. Fix is correct either way.

### Stage 2.5d (retry, PASSED): Smoke retest after C3.6

```
src.* aliases: 81 (was 75 pre-fix; +6)
agent_layer.* aliases: 28 (was 22 pre-fix; +6; matches expected 1+27)
walk_failures: 0
verify_no_legacy_modules(np.array): []
```

All 27 children + 1 top-level alias = 28 total. Clean.

### Stage 3: Build fixture

Source: `outputs/run9_ready/splits/X_test.parquet` (1.43 MB; 349067 x 78).
Sample: `df.head(8).to_parquet(...)` -> `tests/fixtures/migration_smoke.parquet`
(48830 B, 8 x 78). Live 78-col schema.

Note: a bogus `WARN: column count 81 != 78` printed in the batch output;
PowerShell `-match` against a multi-line string array does not populate
`$Matches`. The "81" was the stale `SRC=81` capture from the prior smoke
test. Fixture has 78 columns (verified by the python `COLS=78` output).

### Stage 4: Final readiness

- TARGETS: 5/5 exist
- DELETIONS: 1/1 exist
- FIXTURE: EXISTS at `tests/fixtures/migration_smoke.parquet`
- End-to-end load of `outputs/run9_ready/scaler.joblib` (4 KB) via
  `install_compat_aliases` + `joblib.load`:
  `TYPE=StandardScaler|MOD=sklearn.preprocessing._data|LEGACY=[]`

All preconditions for Stage 5 (real C4 run) satisfied.

### Stage A + B: Commits

`agent_data/c4_batch_commits.ps1` (in 15.6s):

- Stage A: stage 9 files (1 new + 8 modified), commit `e0f4c6e`, push.
  `Local HEAD == Origin HEAD` verified.
- Stage B: stage 2 files (1 modified + 1 force-added fixture; the fixture's
  parent dir `tests/fixtures/` is gitignored — auto-detected via
  `git check-ignore`, force-add applied), commit `e34ce7b`, push.
  `Local HEAD == Origin HEAD` verified.

Final `git status --short`: empty.

## Outstanding

- **CI verification**: at session close, `e0f4c6e` was `in_progress` (4s in);
  `e34ce7b` had not yet appeared in `gh run list`. Both must turn green
  before Stage 5.
- **Stage 5**: the real C4 pickle migration. 3.5 - 5.5 hr wall-clock; schedule
  for a session with no other heavy laptop workload, laptop plugged in,
  full session block of 4 - 6 hr available. Standing rule: NEVER on Vast.ai;
  these joblibs only exist locally; this is local code/data manipulation,
  not training.

## Cross-references

- INCIDENT: `docs/incidents/INCIDENT_2026-05-09_c1-c3-sweep-misses.md`
- CHANGELOG: `docs/CHANGELOG.md` (this date's entry)
- Spec: `docs/hypotheses/HYP_consolidate-package-layout.md` (C1, C2, C3, C4)
- Operational tooling (`agent_data/`, NOT in repo):
  - `c4_fix_install_compat.py` (Stage 2.5b L119 patch)
  - `c4_diagnose_walk.py` (Stage 2.5c diagnostic)
  - `c4_fix_bare_imports.py` (Stage 2.5e C3.6 hotfix sweep)
  - `c4_batch_C36_through_4.ps1` (the integrated workflow batch)
  - `c4_batch_commits.ps1` (the two-commit push batch)